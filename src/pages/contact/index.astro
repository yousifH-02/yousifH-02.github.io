---
const email = "YOUSIFMOYASER@GMAIL.COM";
const phonePretty = "832-217-7825";
const phoneDial = "8322177825";

const linkedinUrl = "https://www.linkedin.com/in/yousif-husni-336b98372";
const instagramHandle = "@YXSF.H";
const instagramUrl = "https://www.instagram.com/YXSF.H/";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Yousif Husni | Contact</title>
    <style>
      :root { --bg:#0b0f17; --card:#0f172a; --text:#e5e7eb; --muted:#9ca3af; --line: rgba(255,255,255,.10); }
      *{ box-sizing:border-box; }
      body{
        margin:0; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        background:
          radial-gradient(1200px 600px at 10% 0%, rgba(147,197,253,.14), transparent 60%),
          radial-gradient(900px 500px at 90% 10%, rgba(167,139,250,.10), transparent 55%),
          var(--bg);
        color: var(--text);
      }
      a{ color: inherit; text-decoration:none; }
      .wrap{ max-width: 980px; margin: 0 auto; padding: 28px 18px 54px; }
      .top{
        display:flex; justify-content:space-between; align-items:center; gap:12px; flex-wrap:wrap;
        border:1px solid var(--line); border-radius:16px; padding:12px 14px;
        background: rgba(17,24,39,.35); backdrop-filter: blur(10px);
      }
      .top h1{ margin:0; font-size: 18px; letter-spacing: .2px; }
      .home{ color:#93c5fd; font-size: 13px; }

      .intro{
        margin-top: 14px;
        border:1px solid var(--line); border-radius:18px; padding:16px;
        background: rgba(15,23,42,.35);
      }
      .intro p{ margin:0; color: var(--muted); line-height:1.55; }

      .grid{
        margin-top: 12px;
        display:grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 12px;
      }
      @media (max-width: 760px){
        .grid{ grid-template-columns: 1fr; }
      }

      .card{
        border:1px solid var(--line); border-radius:18px; padding:14px;
        background: rgba(15,23,42,.35);
      }
      .label{ font-size:12px; color: var(--muted); margin:0 0 6px; }
      .value{ margin:0; font-size:15px; }
      .hint{ margin:8px 0 0; font-size:12px; color: var(--muted); line-height:1.45; }

      .actions{ margin-top: 10px; display:flex; gap:10px; flex-wrap:wrap; }
      .btn{
        padding: 9px 11px;
        border-radius: 14px;
        border: 1px solid rgba(255,255,255,.12);
        background: rgba(255,255,255,.04);
        font-size: 13px;
        display:inline-flex; align-items:center; gap:10px;
      }
      .btn:hover{ border-color: rgba(255,255,255,.22); }

      .footer{
        margin-top: 14px;
        color: var(--muted);
        font-size: 12px;
        display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap;
      }
      .footer a{ color: #93c5fd; }
    </style>
  </head>

  <body>
    <main class="wrap">
      <header class="top">
        <h1>Contact</h1>
        <a class="home" href="/">← Home</a>
      </header>

      <section class="intro">
        <p>
          For business inquiries, the best way to reach me is by email.
          During the holiday season, please allow <strong>2–3 business days</strong> for a response.
          Phone calls are a last resort for time-sensitive, professional business matters.
        </p>
      </section>

      <section class="grid">
        <div class="card">
          <p class="label">Email (preferred)</p>
          <p class="value">{email}</p>
          <p class="hint">Best for business inquiries. Replies may take 2–3 business days during the holiday season.</p>
          <div class="actions">
            <a class="btn" href={`mailto:${email}`}>Email me →</a>
          </div>
        </div>

        <div class="card">
          <p class="label">LinkedIn</p>
          <p class="value">yousif-husni-336b98372</p>
          <p class="hint">Best for professional networking and internship opportunities.</p>
          <div class="actions">
            <a class="btn" href={linkedinUrl} target="_blank" rel="noopener noreferrer">Open LinkedIn →</a>
          </div>
        </div>

        <div class="card">
          <p class="label">Instagram</p>
          <p class="value">{instagramHandle}</p>
          <p class="hint">For quick updates and casual contact.</p>
          <div class="actions">
            <a class="btn" href={instagramUrl} target="_blank" rel="noopener noreferrer">Open Instagram →</a>
          </div>
        </div>

        <div class="card">
          <p class="label">Phone (last resort)</p>
          <p class="value">{phonePretty}</p>
          <p class="hint">Use only if email isn’t an option and the matter is time-sensitive.</p>
          <div class="actions">
            <a class="btn" href={`tel:${phoneDial}`}>Call →</a>
          </div>
        </div>
      </section>

      <footer class="footer">
        <div>© {new Date().getFullYear()} Yousif Husni</div>
        <div>
          <a href={linkedinUrl} target="_blank" rel="noopener noreferrer">LinkedIn</a> ·
          <a href={instagramUrl} target="_blank" rel="noopener noreferrer">Instagram</a>
        </div>
      </footer>
    </main>
  </body>
</html>